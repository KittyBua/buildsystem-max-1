--- a/src/Makefile.in
+++ b/src/Makefile.in
@@ -136,7 +136,6 @@
 
 
 ifeq ($(STATIC),1)
-	CFLAGS+=-static
 	LDFLAGS+=$(addsuffix .a,$(addprefix -l:lib,$(LIBS)))
 else
 	LDFLAGS+=$(addprefix -l,$(LIBS))
